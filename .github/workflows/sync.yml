name: Auto Sync & Tag

on:
  push:
    branches:
      - main

# Restrict permissions as much as possible
permissions:
  contents: write

jobs:
  sync-and-tag:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    if: "!contains(github.event.head_commit.message, 'chore: prepare v')"
    steps:
      - name: Harden Runner
        uses: step-security/harden-runner@0080882f63886ee327175c92c554523315e07671 # v2.10.2
        with:
          egress-policy: audit

      - uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Setup Node
        uses: actions/setup-node@3932cf5a60c0f9a4a621746c19a9d70104961559 # v4.1.0
        with:
          node-version: 20

      - name: Sync Versions
        run: node scripts/sync-version.js

      - name: Check for version change
        id: version_check
        run: |
          VERSION=$(node -p "require('./package.json').version")
          echo "current_version=$VERSION" >> $GITHUB_OUTPUT
          if git rev-parse "v$VERSION" >/dev/null 2>&1; then
            echo "exists=true" >> $GITHUB_OUTPUT
          else
            echo "exists=false" >> $GITHUB_OUTPUT
          fi

      - name: Commit sync if needed
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add Cargo.toml
          git diff --quiet && git diff --staged --quiet || (git commit -m "chore: sync version to Cargo.toml [skip ci]" && git push)

      - name: Create and Push Tag
        if: steps.version_check.outputs.exists == 'false'
        run: |
          VERSION=${{ steps.version_check.outputs.current_version }}
          git tag "v$VERSION"
          git push origin "v$VERSION"
