[package]
name = "gensense"
version = "0.1.0"
authors = ["Friehub Developers"]
edition = "2021"
license = "Proprietary"
publish = false

[dependencies]
tree-sitter = "0.23"
tree-sitter-rust = { version = "0.23", optional = true }
tree-sitter-typescript = { version = "0.23", optional = true }
tree-sitter-solidity = { version = "1.2", optional = true }
tree-sitter-yaml = "0.7"
tree-sitter-javascript = { version = "0.23", optional = true }
anyhow = "1.0"
thiserror = "1.0"
tracing = { version = "0.1", features = ["attributes"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
regex = "1"
walkdir = "2"
once_cell = "1.19"
rayon = "1.8"
diff = { version = "0.1", optional = true }
include_dir = { version = "0.7", features = ["glob"] }
glob = "0.3"

[features]
default = ["rust", "typescript", "fingerprinting", "remediation"]
rust = ["dep:tree-sitter-rust"]
typescript = ["dep:tree-sitter-typescript", "dep:tree-sitter-javascript"]
solidity = ["dep:tree-sitter-solidity"]
fingerprinting = []
remediation = ["dep:diff"]
full = ["rust", "typescript", "solidity", "fingerprinting", "remediation"]

[dev-dependencies]
tempfile = "3"
