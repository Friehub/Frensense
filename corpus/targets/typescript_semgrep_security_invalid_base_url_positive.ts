// Vulnerable: The 'baseURL' is invalid. This may cause links to not work if deployed. Include the scheme (e.g., http:// or https://).
// Pattern: baseURL = "..."
function vulnerable() {
  // TODO: implement pattern match
}
