// Vulnerable: The value of `$X` is being ignored and will be used in the conditional test
// Pattern: {'pattern': 'if ($X=true) { ... }'} | {'pattern': 'if ($X=false) { ... }'}
function vulnerable() {
  // TODO: implement pattern match
}
