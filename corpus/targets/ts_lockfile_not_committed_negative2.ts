// SAFE alternative: use npm ci in CI to enforce exact lockfile resolution
// package-lock.json committed and CI uses:
// npm ci  # installs exact versions from lockfile, fails if lockfile is out of sync
