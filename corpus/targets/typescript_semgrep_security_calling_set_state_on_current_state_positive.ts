// Vulnerable: Calling setState on the current state is always a no-op. Did you mean to change the state like $Y(!$X) instead?
// Pattern: $Y($X);
function vulnerable() {
  // TODO: implement pattern match
}
